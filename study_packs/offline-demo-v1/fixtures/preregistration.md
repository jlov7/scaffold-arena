# Offline fixture demonstration preregistration

This bundled StudyPack demonstrates protocol-v1 import, freezing, factor expansion,
and deterministic evaluation using only synthetic recorded fixtures. It is not a
provider run, a benchmark result, or evidence of causal performance.

Expected controls: the positive fixture passes; the negative fixture fails the
answer and state checks; the anti-cheat fixture triggers the fixture-only claim
ceiling rule.
